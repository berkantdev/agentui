# `@berkantdev/agentui-core`

<!-- TODO(docs-phase-2): Full API reference for @berkantdev/agentui-core
  - Types: A2UIComponent, A2UIComponentType (+ A2UI_COMPONENT_TYPES const),
    A2UIDataModel, A2UIMessage (+ per-type interfaces), AgentUIAdapter<T>,
    AdapterDefaults, AdapterOptions<T>, A2UIMessageLegacy
  - Schemas: A2UIComponentTypeSchema, A2UIComponentSchema, A2UIDataModelSchema,
    A2UIMessageSchema (+ the four per-message schemas)
  - Parsers: parseMessage(raw), parseComponent(input) — inputs, failure modes,
    example with try/catch-free control flow
  - Factories: createAdapter<TComponent>(name, base, options), defaultAdapter
  - Each entry: signature, description, code example, "see also" cross-links
-->

::: info Placeholder
Full reference pending the next documentation PR.

Every public export of `@berkantdev/agentui-core` has JSDoc on its
declaration, which your editor will surface on hover. Source of truth:
[`packages/core/src/index.ts`](https://github.com/berkantdev/agentui/tree/main/packages/core/src/index.ts).

Until this page is filled in, the
[Getting Started guide](/guide/getting-started) has runnable examples
of the most common APIs: `parseMessage`, `createAdapter`, and the
message types.
:::
